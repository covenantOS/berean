[package]
name = "berean-desktop"
version = "0.1.0"
description = "Berean desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[lib]
name = "berean_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
